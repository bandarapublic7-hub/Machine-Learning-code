# Pandas Basics and IO

This is my quick note for Pandas Basics and IO.

Bring raw data into pandas first, because every ML workflow starts with readable tables.

## Main types
- Pandas Series and DataFrame basics
- CSV loading
- JSON loading
- SQL querying with sqlite
- API-style payload flattening
- HTML table reading

## What this topic does
This topic shows how to move data from files, JSON, SQL, and simple web tables into DataFrames so the rest of your ML work has one clean structure.

## Simple real-life example
A sales team sends a CSV, the app team sends JSON, and finance keeps data in SQL. Pandas lets you open all three in the same shape.

